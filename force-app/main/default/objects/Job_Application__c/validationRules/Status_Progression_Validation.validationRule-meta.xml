<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Status_Progression_Validation</fullName>
    <active>true</active>
    <description>Validates proper status progression and prevents invalid status changes</description>
    <errorConditionFormula>AND(
    ISCHANGED(Status__c),
    OR(
        AND(
            ISPICKVAL(PRIORVALUE(Status__c), "Closed"),
            NOT(ISPICKVAL(Status__c, "Applied"))
        ),
        AND(
            ISPICKVAL(PRIORVALUE(Status__c), "Accepted"),
            NOT(ISPICKVAL(Status__c, "Closed"))
        ),
        AND(
            ISPICKVAL(Status__c, "Negotiating"),
            NOT(ISPICKVAL(PRIORVALUE(Status__c), "Interviewing"))
        )
    )
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>Invalid status change. Please follow proper progression: Applied → Interviewing → Negotiating → Accepted/Closed. Closed applications can only be reopened to Applied status.</errorMessage>
</ValidationRule>
